import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
// optional: import "./index.css"; // keep if you have any custom css, not required for Tailwind CDN

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
